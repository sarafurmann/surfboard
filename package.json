{
  "name": "surfer",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean-cache": "rm -rf dist .parcel-cache",
    "dev": "npm run clean-cache && parcel ./src/index.html",
    "build": "rm -rf dist && parcel build ./src/index.html --public-url https://sarafurmann.github.io/surfboard/",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.3.2",
    "parcel": "^2.3.2",
    "posthtml-include": "^1.7.2"
  },
  "dependencies": {
    "gh-pages": "^3.2.3"
  }
}
